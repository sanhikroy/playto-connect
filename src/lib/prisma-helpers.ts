import { PrismaClient } from '@prisma/client'
import prisma from '@/lib/prisma'

/**
 * Use the prisma client directly
 * With newer versions of Prisma, the client should have all necessary types
 */
export default prisma 